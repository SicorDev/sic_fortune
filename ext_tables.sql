CREATE TABLE tx_sicfortune_domain_model_fortune (
    text text NOT NULL DEFAULT '',
    author varchar(255) NOT NULL DEFAULT '',
    lang varchar(2) NOT NULL DEFAULT '',
    showon varchar(10) NOT NULL DEFAULT ''
);
